<odoo>
    <!-- Vista tree para mail.activity -->
    <record id="view_mail_activity_tree_systray" model="ir.ui.view">
        <field name="name">mail.activity.tree.systray</field>
        <field name="model">mail.activity</field>
        <field name="arch" type="xml">
            <tree string="Actividades"
                  decoration-danger="state == 'overdue'"
                  decoration-warning="state == 'today'">
                <field name="state"/>
                <field name="date_deadline"/>
                <field name="activity_type_id"/>
                <field name="summary"/>
                <field name="user_id"/>
                <field name="res_model_id"/>
                <field name="res_name" string="Documento"/>
            </tree>
        </field>
    </record>

    <!-- Acción que abre en lista -->
    <record id="action_mail_activity_list_systray" model="ir.actions.act_window">
        <field name="name">Actividades</field>
        <field name="res_model">mail.activity</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_mail_activity_tree_systray"/>
        <field name="domain">[]</field>   <!-- el systray inyecta su propio domain -->
        <field name="context">{}</field>  <!-- permite que el systray pase res_model/domain -->
    </record>
</odoo>